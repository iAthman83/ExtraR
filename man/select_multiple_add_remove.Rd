% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/other_responses.R
\name{select_multiple_add_remove}
\alias{select_multiple_add_remove}
\title{Generate Cleaning Log entries for select_multiple Modification}
\usage{
select_multiple_add_remove(
  el,
  to_remove,
  to_add = c(),
  exclusive_options = c("none", "none_of_the_above", "dont_know", "dk", "pnd",
    "no_other_choices")
)
}
\arguments{
\item{el}{A list containing \code{uuid}, \code{ref_name} (main question name), and \code{change_type}.}

\item{to_remove}{A character vector of choice names to remove.}

\item{to_add}{A character vector of choice names to add.}

\item{exclusive_options}{A character vector of choice names that are exclusive
(e.g., "none", "dont_know"). If selected, all other choices will be deselected.}
}
\value{
A data frame containing cleaning log entries with columns \code{uuid}, \code{question},
\code{old_value}, \code{new_value}, and \code{change_type}.
}
\description{
Computes the necessary cleaning log entries when choices are added and/or removed
from a \code{select_multiple} question. Automatically updates both the concatenated
value column and the individual binary option columns (e.g. \code{question/choice}).
}
