% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kobo_api.R
\name{kobo_download_media}
\alias{kobo_download_media}
\title{Download KoBo Submission Media}
\usage{
kobo_download_media(
  asset_id,
  server_url = "https://kobo.impact-initiatives.org",
  media_dir = "images/",
  enum_col = "enum",
  zip_output = TRUE
)
}
\arguments{
\item{asset_id}{String. The KoBo asset/form unique identifier (UID).}

\item{server_url}{String. The base URL of your KoBo server instance. Default is "https://kobo.impact-initiatives.org".}

\item{media_dir}{String. The target base directory path to save downloaded images. Default is "images/".}

\item{enum_col}{String. The exact field/column name in the form tracking the enumerator's identity. Default is "enum".}

\item{zip_output}{Logical. If TRUE, bundles all downloaded subfolders into a single zip archive. Default is TRUE.}
}
\value{
Invisibly returns the base media directory path string.
}
\description{
Downloads all image and media attachments for each submission from the KoBo server.
Files are automatically renamed based on their XLSForm question/indicator name
(e.g., photo_trash.jpg) instead of keeping generic timestamps. Folders are structured
by enumerator and submission UUID (enum_uuid) to maintain data tracking.
Fetches data directly from the KoBo JSON API and skips already-downloaded files automatically.
}
