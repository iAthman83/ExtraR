% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_group_analysis.R
\name{run_group_analysis_pipeline}
\alias{run_group_analysis_pipeline}
\title{Run Full Group Analysis Pipeline}
\usage{
run_group_analysis_pipeline(
  dataset,
  loa,
  group_variables = c("Overall"),
  tool_survey,
  tool_choices,
  weight_column = "weights",
  strata_column = "sample_location",
  value_columns = c("stat", "n", "n_total"),
  extra_columns = NULL
)
}
\arguments{
\item{dataset}{The survey dataset.}

\item{loa}{List of Analysis (LOA) as a data frame. This is typically loaded from an Excel file
containing the full list of variables to analyse, their analysis types, and metadata columns.
Common optional metadata columns that can be passed via \code{extra_columns} include
\code{sector}, \code{indicator}, and \code{subset_indicator}.}

\item{group_variables}{A character vector of group variables to iterate over. Defaults to c("Overall").}

\item{tool_survey}{The Kobo tool survey sheet.}

\item{tool_choices}{The Kobo tool choices sheet.}

\item{weight_column}{The column name for survey weights. Defaults to "weights".}

\item{strata_column}{The column name for survey strata. Defaults to "sample_location".}

\item{value_columns}{Columns to extract from results. Defaults to c("stat", "n", "n_total").}

\item{extra_columns}{Extra LOA metadata columns to carry through to the output (e.g. \code{c("sector", "indicator", "subset_indicator")}). Defaults to NULL.}
}
\value{
A list containing \code{combined_results} (the merged dataset spanning all group variables) and \code{uuid_table} (the baseline UUID mappings).
}
\description{
This function iterates over a list of group variables, performs the standard analysis
for each group, adds labels, and pivots the results. For the first group in the list
(usually 'Overall'), it assigns a unique UUID to each result row. For all subsequent
group variables, it matches the results back to the original UUIDs using \code{analysis_var},
\code{analysis_type}, and \code{analysis_var_value}. This avoids the need to repeatedly
read and write to an Excel file during the loop, producing one massive dataset spanning all groups.
}
