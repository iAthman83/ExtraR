# Insert Empty Rows into a Data Frame

Internal helper function to insert empty rows between grouped elements.

## Usage

``` r
add_empty_rows_between_groups(df, col)
```

## Arguments

- df:

  The data frame.

- col:

  The column used to identify groups.

## Value

A modified dataset with empty rows separating distinct groups.
